akka {
  loglevel = DEBUG
  //loglevel = "DEBUG"
  actor {
    provider = remote
  }
  remote {
    enabled-transports = ["akka.remote.netty.tcp"]
    netty.tcp {
//      hostname = "0.0.0.0"
      port = 2552
    }
 }
}

http {
  interface = "0.0.0.0"
  port = 9000
}

